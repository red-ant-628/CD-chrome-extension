var images = document.getElementsByTagName('img');
for (var i = 0, l = images.length; i < l; i++) {
  images[i].src = 'http://placekitten.com/' + images[i].width + '/' + images[i].height;
}
var string = '<!DOCTYPE html><html><head></head><body>content</body></html>';
var doc = new DOMParser().parseFromString(string, 'text/html');
doc.body.innerHTML;
wordcount = string.length;

$ npm install --save saurus

for (wordcount) {
var thesaurus = require('saurus');

thesaurus(string(1).then(matches => console.log(matches));
 
document.getElementById("output").innerHTML=thesaurus;
}
